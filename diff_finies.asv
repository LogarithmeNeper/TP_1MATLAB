function [valeur] = diff_finies(A,i,j)
% Fonction différente
% i : lignes; j : colonnes
% on traite les cas particuliers dans la fonction
vCase=0;
vGauche=0;
vDroite=0;
vHaut=0;
vBas=0;


% cas ligne du haut
if(i==1)
    vHaut=vCase;
elseif(i==20)
    vBas=vCase;
end

if(j==1)
    if(j<=10)
        vGauche=
   
end
   
end

end

